# ==========================================
# 엘더베리 프론트엔드 - 개발환경 Dockerfile
# Node.js 22 + Vite dev server
# ==========================================

FROM node:22-alpine

# 작업 디렉토리 설정
WORKDIR /app

# 패키지 관리자 업그레이드
RUN npm install -g npm@latest

# 패키지 파일 복사 및 의존성 설치
COPY package*.json ./
RUN npm install --legacy-peer-deps

# 소스 코드 복사
COPY . .

# 포트 노출
EXPOSE 5173

# 개발 서버 실행
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]