# ==========================================
# 엘더베리 프로젝트 - Plain Java Dockerfile
# Simple Java server for testing
# ==========================================

FROM openjdk:21-jre-slim

# 작업 디렉토리 설정
WORKDIR /app

# 컴파일된 클래스 파일 복사
COPY build/classes/ classes/

# 포트 노출
EXPOSE 8080

# Plain Java 서버 실행
CMD ["java", "-cp", "classes", "com.globalcarelink.PlainJavaServer"]