# =================================================================
# Elderberry 프로젝트 환경 변수 설정 예시
# =================================================================
# 이 파일을 .env로 복사하고 실제 값들로 수정하세요
# 주의: .env 파일은 Git에 추가하지 마세요! (.gitignore에 포함됨)

# =================================================================
# 🔐 보안 설정 (필수)
# =================================================================

# JWT 토큰 암호화 키 (최소 32자 이상, 운영환경에서는 복잡한 키 사용)
JWT_SECRET=your-super-secure-jwt-secret-key-at-least-32-characters-long-for-production

# JWT 토큰 만료 시간 (밀리초)
JWT_ACCESS_EXPIRATION=3600000   # 1시간
JWT_REFRESH_EXPIRATION=86400000 # 24시간

# =================================================================
# 🛡️ 프로덕션 보안 강화 설정
# =================================================================

# 보안 모드 활성화 (프로덕션에서는 true로 설정)
SECURITY_PRODUCTION_MODE=false

# CORS 허용 도메인 (쉼표로 구분)
# 개발환경: http://localhost:3000,http://localhost:5173
# 프로덕션: https://elderberry.app,https://globalcarelink.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# =================================================================
# 🗄️ 데이터베이스 설정
# =================================================================

# H2 데이터베이스 인증 정보
DB_USERNAME=sa
DB_PASSWORD=your-secure-db-password

# =================================================================
# 🌐 공공데이터 API 설정 (필수)
# =================================================================

# 공공데이터포털에서 발급받은 API 키
PUBLIC_DATA_API_KEY=your-public-data-api-key-from-data-go-kr

# 공공데이터 API 기본 URL (일반적으로 변경 불필요)
PUBLIC_DATA_BASE_URL=https://apis.data.go.kr

# =================================================================
# 🤖 챗봇 서비스 설정
# =================================================================

# 챗봇 서비스 활성화 여부
CHATBOT_ENABLED=true

# 챗봇 서비스 URL
CHATBOT_BASE_URL=http://localhost:8000
CHATBOT_SERVICE_URL=http://localhost:8000

# 챗봇 서비스 타임아웃 설정 (밀리초)
CHATBOT_CONNECT_TIMEOUT=10000
CHATBOT_READ_TIMEOUT=30000
CHATBOT_TIMEOUT=10

# =================================================================
# 🌐 Redis 설정 (JWT 블랙리스트용)
# =================================================================

# Redis 서버 설정
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DATABASE=0
REDIS_TIMEOUT=2000ms

# =================================================================
# 📝 설정 가이드
# =================================================================

# 1. 공공데이터 API 키 발급 방법:
#    - https://www.data.go.kr 접속
#    - 회원가입 및 로그인
#    - 개발계정 신청
#    - API 신청 후 키 발급받기

# 2. JWT 시크릿 생성 방법:
#    - 온라인 랜덤 문자열 생성기 사용
#    - 최소 32자 이상 영문/숫자/특수문자 조합
#    - 절대로 소스코드에 하드코딩하지 말 것

# 3. 프로덕션 보안 설정:
#    - SECURITY_PRODUCTION_MODE=true로 설정
#    - CORS_ALLOWED_ORIGINS를 실제 도메인으로 제한
#    - H2 콘솔 및 개발 도구 자동 비활성화
#    - Redis를 통한 JWT 블랙리스트 영구 저장

# 4. 운영환경 배포 시:
#    - 모든 기본값을 실제 운영 값으로 변경
#    - .env 파일 권한을 600으로 설정 (chmod 600 .env)
#    - 환경변수는 배포 도구를 통해 안전하게 주입

# =================================================================
# 🔒 보안 주의사항
# =================================================================

# ⚠️  절대로 이 파일을 Git에 커밋하지 마세요!
# ⚠️  .env 파일은 .gitignore에 반드시 포함시키세요!
# ⚠️  운영환경에서는 더 강력한 암호화 키를 사용하세요!
# ⚠️  API 키는 정기적으로 갱신하세요!
# ⚠️  프로덕션에서는 SECURITY_PRODUCTION_MODE=true 필수!
# ⚠️  CORS 설정을 실제 도메인으로 제한하세요!
# ⚠️  Redis 비밀번호는 프로덕션에서 반드시 설정하세요!